import { Box } from "@mui/material";

const DemoPage = () => {
  return (
    <Box
      sx={{ display: "flex", flexDirection: "column", flex: 1, py: 4 }}
    ></Box>
  );
};

export default DemoPage;
